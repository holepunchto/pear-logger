{
  "name": "pear-logger",
  "version": "1.0.1",
  "main": "index.js",
  "type": "commonjs",
  "description": "Pear logger",
  "license": "Apache-2.0",
  "files": [],
  "imports": {
    "util": {
      "bare": "bare-format"
    }
  },
  "scripts": {
    "format": "prettier --write . && lunte --fix",
    "lint": "prettier --check . && lunte",
    "test:gen": "brittle -r test/all.js test/*.test.js",
    "test": "brittle-bare --coverage test/all.js"
  },
  "devDependencies": {
    "brittle": "^3.0.0",
    "lunte": "^1.6.0",
    "prettier": "^3.6.2",
    "prettier-config-holepunch": "^1.0.0"
  },
  "dependencies": {
    "bare-format": "^1.0.1",
    "bare-hrtime": "^2.0.11",
    "bare-tty": "^5.1.0",
    "pear-cmd": "^1.0.0"
  }
}
